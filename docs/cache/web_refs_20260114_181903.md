# Web refs cache 20260114_181903

## Note
- No external web search tool available in this environment.
- Used local CLI help output instead:
  - `codex --help`
  - `codex exec --help`

## Parameters referenced for wrapper defaults
- `--search` (enable web search)
- `--dangerously-bypass-approvals-and-sandbox` (full access)
- `--sandbox`, `--ask-for-approval`, `--model`, `--profile`, `--config`, `--enable`, `--disable`, `--image`, `--add-dir`, `--output-schema`, `--output-last-message`, `--json`, `--color`, `--skip-git-repo-check`
